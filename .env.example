# Bookings/booking API (axios): leave empty to use same-origin /api (Vite proxies to :8080 — recommended for dev).
# Or set full backend URL, e.g. http://localhost:8080  ( /api is appended automatically ).
# VITE_API_BASE_URL=

